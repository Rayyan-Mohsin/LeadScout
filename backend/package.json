{
  "name": "backend",
  "version": "1.0.0",
  "description": "API that finds local businesses without a website by scraping Google Maps",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "p-limit": "^7.3.0",
    "puppeteer": "^25.3.0"
  }
}
